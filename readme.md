1. Создать 3 класса:
    1) ApiMethods, класс уровня данных  - тут будут методы для работы с
       апи(получание/создание/редактррование/удаление записей);
    2) Interface, класс пользовательского интерфейса - тут будут 
       методы которые будут рисовать дом элементы и делать вывод данных;
    3) Controllers, главный класс - этот класс будет контроллером, 
       его цель создавать экземпляры 2х других классов и он будет описывать 
       логику приложения, в нем будут создаваться 2 экземпляра(уровень апи и UI)
       и там сделать метод рендер. который вызывает апи получения данных, 
       и потом рисует контакты через метод UI;

2. Разнести все классы по отдельным файлам;
   
3. Кдасс форми добавления контакта;

